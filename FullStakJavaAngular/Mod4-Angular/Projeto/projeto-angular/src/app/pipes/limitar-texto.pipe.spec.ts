import { LimitarTextoPipe } from './limitar-texto.pipe';

describe('LimitarTextoPipe', () => {
  it('create an instance', () => {
    const pipe = new LimitarTextoPipe();
    expect(pipe).toBeTruthy();
  });
});
