import { SomarArrayPipe } from './somar-array.pipe';

describe('SomarArrayPipe', () => {
  it('create an instance', () => {
    const pipe = new SomarArrayPipe();
    expect(pipe).toBeTruthy();
  });
});
