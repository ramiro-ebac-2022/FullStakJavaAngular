import { OcultarEmailPipe } from './ocultar-email.pipe';

describe('OcultarEmailPipe', () => {
  it('create an instance', () => {
    const pipe = new OcultarEmailPipe();
    expect(pipe).toBeTruthy();
  });
});
